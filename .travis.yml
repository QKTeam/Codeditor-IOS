language: objective-c

osx_image: xcode7.3

before_install:
    - pod --version
    - pod setup --silent > /dev/null
    - pod repo update --silent

script:
    - pos intall
    - xctool -workspace Codeditor-IOS.xcworkspace -scheme 'Codeditor-IOS' -sdk iphonesimulator build
