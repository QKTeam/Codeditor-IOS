language: objective-c

osx_image: xcode7.3

before_install:
    - pod --version
    - pod setup --silent > /dev/null
    - pod repo update --silent

script:
    - pod install
    - xctool -workspace Codeditor-IOS.xcworkspace -scheme 'Codeditor-IOS' -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO
